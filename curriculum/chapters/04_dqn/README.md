# DQN

Status: `usable`

把 tabular Q-learning 过渡到 neural Q-network。

## 怎么学

1. 读 `lesson.md`。
2. 按 `walkthrough.md` 走最小路径。
3. 按 `lab.md` 跑 demo。
4. 做 `exercises.md`。
5. 卡住先看 `hints.md`，再看 `solutions.md`。
6. 填写 `report.md`。

本章还不是 beginner-ready。它可以运行和学习，但解释密度仍然偏高。
