# PyTorch Foundations Report

Use `docs/experiment_report_template.md` for each experiment.

## Current Status

The repository contains the first reusable MLP, checkpoint, export, and ONNX
consistency helpers.
